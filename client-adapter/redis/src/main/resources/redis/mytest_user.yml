dataSourceKey: defaultDS
destination: mryt_vip_t_vip_base_info1
groupId: g1
redisMapping:
  database: mryt_vip  # 数据库名
  table: t_vip_base_info     # 数据库表名
  keyType: SET  #对应redis里面的类型
  key: "'com:mryt:viptests:'+ user_id"  # redis的key
  value: user_id     # 数据库表名和字段名
  condition: "level=2"


# 列子 将t_vip_base_info表中的level=2的数据放入redis中。redis的key：com.mryt.viptests;value是：user_id